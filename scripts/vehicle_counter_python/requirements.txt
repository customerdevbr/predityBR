# Vehicle Counter Backend — Python dependencies
# Install with: pip install -r requirements.txt
# For CPU-only torch (smaller / faster on VPS):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

ultralytics>=8.0.0
supervision>=0.22.0
supabase>=2.0.0
requests>=2.28.0
python-dotenv>=1.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
